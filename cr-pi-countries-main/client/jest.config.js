/** @type {import('jest').Config} */
const config = {
    verbose: true,
    // "setupFilesAfterEnv": [
    //   "<rootDir>/src/setupTests.ts"
    // ]
  };
  
  export default {
    "testEnvironment": "jsdom"
}